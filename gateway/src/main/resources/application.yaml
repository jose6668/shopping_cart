spring:
  application:
    name: gateway

server:
  port: ${SERVER_PORT:8080}

services:
  backend:
    base-url: ${BACKEND_BASE_URL:http://localhost:8081}

management:
  endpoints:
    web:
      exposure:
        include: health,info
